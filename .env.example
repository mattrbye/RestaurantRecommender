NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Use locally or in trusted server environments; do not expose this in the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Enables POST /api/seed so the synthetic restaurant catalog and demo events can be inserted.
ALLOW_DATABASE_SEED=false
